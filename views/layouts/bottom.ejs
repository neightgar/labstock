    </div><!-- /.page-inner -->
  </div><!-- /.main -->

</div><!-- /.app -->

<%- include('../partials/order-modal') %>

<!-- ── Custom Confirm Modal ──────────────────────────────── -->
<div id="confirm-modal" class="modal-overlay" role="dialog" aria-modal="true">
  <div class="modal-box">
    <div class="confirm-body">
      <div class="confirm-icon">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/></svg>
      </div>
      <div class="confirm-title"><%= t('confirm.title') %></div>
      <div class="confirm-message" id="confirm-message"><%= t('confirm.delete') %></div>
      <div class="confirm-actions">
        <button id="confirm-no"  class="btn btn-ghost"><%= t('confirm.no') %></button>
        <button id="confirm-yes" class="btn btn-danger"><%= t('confirm.yes') %></button>
      </div>
    </div>
  </div>
</div>

<% if (typeof extraScripts !== 'undefined') { %>
  <%- extraScripts %>
<% } %>

<script src="/js/app.js"></script>
</body>
</html>
